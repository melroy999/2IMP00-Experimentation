{% if model | length > 0 %}
if ({{model[0] | get_guard_statement}}) {
    {{model[0] | get_choice_structure(add_counter) | indent(4, False)}}
}{% for i in range(1, model | length) %} else if({{model[i] | get_guard_statement}}) {
    {{model[i] | get_choice_structure(add_counter) | indent(4, False)}}
}
{%- endfor %} else {
    return false;
}
{%- endif %}