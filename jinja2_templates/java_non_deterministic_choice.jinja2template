java_choice = java_randomGenerator.nextInt({{model | length}});
switch(java_choice) {
{% for i in range(0, model | length) %}
{% set choice = model[i] %}
    case {{i}}:
        {{choice | get_choice_structure(add_counter, sm) | indent(8, False)}}
{% endfor %}
}